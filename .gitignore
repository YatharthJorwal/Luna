# Frontend
node_modules/
/dist

# Tauri / Rust
src-tauri/target/

# Python
orchestrator/venv/
orchestrator/__pycache__/
orchestrator/**/__pycache__/
orchestrator/*.pyc

# OS
.DS_Store
Thumbs.db

# Proprietary, downloaded manually per README -- never commit this
public/live2dcubismcore.min.js

# Personal launcher scripts -- hardcode machine-specific absolute paths
# (your GPT-SoVITS install location, etc.), so these stay local-only, same
# as config.yaml's real ref_audio_path/prompt_text values.
start-luna.bat
src-tauri/launcher.local.txt
src-tauri/gen/

# Output of spawn_backend_processes() in src-tauri/src/lib.rs -- generated
# every run, not source.
/logs/

# Phase 3 memory DB -- the user's own facts/episodes, created on first use,
# not source and not something to ship/share between machines.
orchestrator/data/
 
 