fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pyttsx3>=2.90,<3.0
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0
faster-whisper>=1.0,<2.0
sqlite-vec>=0.1,<0.2
# Phase 4 -- tools/vision.py. Pillow's ImageGrab.grab() for
# capture_screen (Windows/macOS-native, no extra system install needed
# on either); pyperclip for read_clipboard (Windows-native via its
# built-in win32 clipboard support, also no extra system install needed
# on this project's target platform).
Pillow>=10.0,<11.0
pyperclip>=1.9,<2.0
