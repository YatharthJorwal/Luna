# Dev/test-only -- not needed to run the app itself (see requirements.txt
# for that). Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
